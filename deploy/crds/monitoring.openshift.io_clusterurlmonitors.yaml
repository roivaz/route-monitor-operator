---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.1
  name: clusterurlmonitors.monitoring.openshift.io
spec:
  group: monitoring.openshift.io
  names:
    kind: ClusterUrlMonitor
    listKind: ClusterUrlMonitorList
    plural: clusterurlmonitors
    singular: clusterurlmonitor
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ClusterUrlMonitor is the Schema for the clusterurlmonitors API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ClusterUrlMonitorSpec defines the desired state of ClusterUrlMonitor
            properties:
              domainExtractPattern:
                default: ^[^.]+\.(.+)$
                description: |-
                  DomainExtractPattern is a regex pattern used to extract the valid domain from the cluster's base domain.
                  The pattern should contain one capture group that matches the desired domain part.
                  If not specified, defaults to extracting everything after the first subdomain (e.g., "rosa" or "api").
                type: string
              domainRef:
                default: infra
                description: |-
                  ClusterDomainRef defines the object used determine the cluster's domain
                  By default, 'infra' is used, which references the 'infrastructures/cluster' object
                enum:
                - infra
                - hcp
                type: string
              insecureSkipTLSVerify:
                description: |-
                  InsecureSkipTLSVerify indicates that the blackbox exporter module used to probe this route
                  should *not* use https
                type: boolean
              port:
                description: Port specifies the port to use when constructing the
                  URL to monitor
                type: string
              prefix:
                description: Prefix is prepended to the cluster domain when constructing
                  the URL to monitor
                type: string
              serviceMonitorType:
                default: monitoring.coreos.com
                description: ServiceMonitorType dictates the type of ServiceMonitor
                  the RouteMonitor should create
                enum:
                - monitoring.coreos.com
                - monitoring.rhobs
                type: string
              skipPrometheusRule:
                description: |-
                  SkipPrometheusRule instructs the controller to skip the creation of PrometheusRule CRs.
                  One common use-case for is for alerts that are defined separately, such as for hosted clusters.
                type: boolean
              slo:
                description: Service level objective for the monitor
                properties:
                  targetAvailabilityPercent:
                    description: TargetAvailabilityPercent defines the percent number
                      to be used
                    type: string
                required:
                - targetAvailabilityPercent
                type: object
              suffix:
                description: Suffix is appended to the cluster domain when constructing
                  the URL to monitor
                type: string
            type: object
          status:
            description: ClusterUrlMonitorStatus defines the observed state of ClusterUrlMonitor
            properties:
              errorStatus:
                description: ErrorStatus contains error information if the ClusterUrlMonitor
                  is in an error state
                type: string
              prometheusRuleRef:
                description: PrometheusRuleRef contains the reference to the PrometheusRule
                  created for this ClusterUrlMonitor
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                required:
                - name
                - namespace
                type: object
              serviceMonitorRef:
                description: ServiceMonitorRef contains the reference to the ServiceMonitor
                  created for this ClusterUrlMonitor
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                required:
                - name
                - namespace
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
